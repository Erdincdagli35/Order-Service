server:
port: ${PORT:8080}
address: 0.0.0.0

spring:
  datasource:
    url: jdbc:postgresql://hopper.proxy.rlwy.net:12135/railway
    username: postgres
    password: yzUZHXEUVmJdPmEfjImzaoefoyRMloQo
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    bootstrap-servers: kafka.railway.internal:29092

    producer:
      kafka:
        consumer:
          key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
          value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
          properties:
            spring.json.trusted.packages: '*'

    properties:
      spring.json.add.type.headers: false
#  mail:
#    host: smtp.gmail.com
#    port: 465
#    username: ${MAIL_USERNAME}
#    password: ${MAIL_PASSWORD}
#    properties:
#      mail:
#        smtp:
#          auth: true
#          starttls:
#            enable: true
#          ssl:
#            trust: smtp.gmail.com
product:
  service:
    url: https://productservice.up.railway.app
